import styled from "styled-components";
import {toRem} from "../../utils/toRem";

export const NewHabitContent = styled.div`
margin-bottom: ${toRem(16)};
`
export const PlusButtonContainer = styled.div`
display: flex;
justify-content: flex-end;
`